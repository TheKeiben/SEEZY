[develop]
# Specifies where to put the scripts when installing in 'develop' (symlink) mode
script_dir=$base/lib/navigation
[install]
# Specifies where to put the scripts when doing a standard full install
install_scripts=$base/lib/navigation
